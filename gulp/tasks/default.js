'use strict';

var gulp  = require('gulp');

//Default task runs when you run "gulp" alone in the terminal (not being used now)
gulp.task('default', function() { 
	console.log('This is the default tasks');
});